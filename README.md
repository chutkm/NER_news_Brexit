# 📰 NER_news_Brexit  
**Named Entity Recognition (NER) on Russian Brexit News using LLM (GigaChat)**

тот проект реализует систему извлечения именованных сущностей (**Named Entity Recognition, NER**) из русскоязычных новостных текстов на тему **Brexit**, с использованием LLM (GigaChat) и строгой оценки качества по эталонной разметке из корпуса **Balto-Slavic NLP Shared Task 2019 (BSNLP-2019)**.

Сущности классифицируются по 5 типам:  
- `PER` — персоны  
- `ORG` — организации  
- `LOC` — локации  
- `EVT` — события  
- `PRO` — продукты (СМИ, ПО и др.)

---

## 📁 Структура проекта
```
NER_news_Brexit/
├── README.md # Этот файл
├── analysis.ipynb # Основной ноутбук с полным пайплайном
├── df_corr.csv # Промежуточный датасет: gold vs. предсказания
└── bsnlp_sample/ # Данные (скачиваются автоматически)
├── raw/
│ └── ru/
└── annotated/
└── ru/
```
---

## 🚀 Установка и запуск

### Требования
- Python ≥ 3.10  
- Библиотеки (см. `!pip install` в начале ноутбука):
  ```
  pandas numpy requests matplotlib seaborn scipy
  ```
  
### 1. Запустите `analysis.ipynb`
Jupyter-ноутбук полностью автономный:

✅ Автоматически скачивает выборку BSNLP-2019 (ru)  
✅ Предобрабатывает тексты (удаление URL, нормализация переносов)  
✅ Загружает эталонную разметку из `.out` файлов  
✅ Формирует промпты для GigaChat (с фокусом на строгую нормализацию и кросс-лингвальные ID)  
✅ Встраивает предсказания (вручную или через API в реальном сценарии)  
✅ Считает **exact-match**, **surface-match**, **macro-F1**, **micro-F1**, **by-type stats**  
✅ Визуализирует результаты и корреляции с длиной текста  
✅ Делает выводы и предлагает улучшения  

> 💡 *В текущей версии предсказания GigaChat загружены вручную как `output_*.py` (в секции ноутбука). В production их можно получать через GigaChat API.*

---

## 🧪 Оценка качества

- **Метрики**:  
- `exact-match` (полное совпадение `<TAB>`-строки)  
- `surface-match` (совпадение упоминания, без учёта леммы/типа/ID)  
- `macro-F1` (усреднённый F1 по классам — **основная метрика**, т.к. есть дисбаланс)  
- `micro-F1` (глобальный F1 по всем сущностям)  

- **Результаты (все документы)**:
- Macro-F1: 0.221
- Micro-F1: 0.218
- Всего gold-сущностей: 88
- Всего предсказаний: 86
- TP/FP/FN: 19 / 67 / 69

- **По типам (только где есть gold)**:
| Тип | F1   | Precision | Recall |
|-----|------|-----------|--------|
| PER | 0.314| 0.333     | 0.296  |
| ORG | 0.182| 0.161     | 0.208  |
| LOC | 0.160| 0.182     | 0.143  |
| EVT | 0.200| 0.250     | 0.167  |
| PRO | 0.250| 0.200     | 0.333  |

🟢 **PRO** (СМИ) — лучший recall (0.33)  
🔴 **LOC/EVT** — худшие показатели: модель пропускает локации и события

---

## 🔍 Ключевые выводы

1. **GigaChat склонен к ложным срабатываниям (FP = 67)** — выдаёт много «лишних» сущностей.
2. **Короткие тексты с упором на персон (PER)** дают лучший F1 (~0.5).
3. **Длинные тексты с событиями (EVT)** — худший сценарий (F1 ↓ до 0.13).
4. **События (Brexit)** и **гео-объекты (LOC)** — системная слабость LLM-подхода без внешних словарей.
5. **Длина текста** не коррелирует со скором (ρ < 0.1), — качество зависит от *тематики* и *композиции сущностей*, а не объёма.

---

## 🛠️ Как улучшить?

| Проблема | Предложение |
|---------|-------------|
| Низкий recall для EVT/LOC | Добавить **ручные словари** (`brexit`, `Великобритания`, `Северная Ирландия` и т.д.) |
| Разные формы одной сущности | Использовать **лемматизацию** (`pymorphy2`) для нормализации surface-форм |
| Много FP (ложных сущностей) | Внедрить **постфильтрацию** через внешние источники (GeoNames, Wikidata) |
| Неустойчивость к падежам | Добавить в промпт примеры **поверхностных вариантов** одного канонического имени |
| Низкий макро-F1 для редких классов | Применить **анализ ошибок per-class** и переобучить/адаптировать промпт отдельно под EVT, LOC |

---

## 📚 Используемые данные

- **BSNLP-2019 Shared Task Sample** (русский подкорпус)  
- URL: https://bsnlp.cs.helsinki.fi/bsnlp-2019/sample_pl_cs_ru_bg.zip  
- Лицензия: свободное использование для исследовательских целей (с указанием источника)  
- Задача: multilingual NER on political crises (Brexit, Czexit и др.)

---


## 📜 Лицензия

Данные BSNLP — используются в исследовательских целях с указанием источника.
